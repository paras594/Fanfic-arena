import styled from "styled-components";
import { colors } from "../../../styles/variables.js";

export const Header = styled.header`
	padding-top: 1rem;
	padding-bottom: 2rem;
	background: ${colors.homepageHeaderBg};
`;
