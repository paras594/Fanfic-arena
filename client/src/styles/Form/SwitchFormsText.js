import styled from "styled-components";
import { colors } from "../variables.js";

const SwitchFormText = styled.p`
	font-size: 1rem;
	font-weight: 600;
	text-align: center;
	margin-top: 2.4rem;
	color: ${colors.switchFormsTextClr};
`;

export default SwitchFormText;
